FROM python:3-alpine
WORKDIR /app

RUN apk --no-cache -q add \
    ffmpeg bash libffi-dev musl-dev gcc


COPY requirements.txt .
RUN pip3 install -r requirements.txt


    
COPY setup.sh .
RUN bash setup.sh
RUN apk del musl-dev gcc
COPY bot bot

CMD ["python3", "-m", "bot"]

